export interface ToolModel {
  toolId?: string;
  toolName: string;
  toolDescription: string;
  toolBrand: string;
  toolModel: string
  toolSerial: string;
  toolStatus: boolean;
  toolImg: string;
}
