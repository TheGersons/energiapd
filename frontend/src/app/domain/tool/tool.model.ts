export interface ToolModel {
  toolId?: string;
  toolName: string;
  toolDescription: string;
  toolBrand: string;
  toolModel: string;
  toolSerial: string;
  toolImg: string;
  toolAvailable: boolean;
  toolCode: string;
  createdAt?: string;
  updatedAt?: string;
}
